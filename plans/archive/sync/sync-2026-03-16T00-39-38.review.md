# Documentation Sync Review — 2026-03-16T00:39:38

## Verdict
**UPDATES_NEEDED**

## Required Updates

### CLAUDE.md
- [x] ALREADY UPDATED: MCP tool count (line 33) updated from "6 tools" to "6 tools always + deep_scan_fuzz when Podman available"
- [x] ALREADY UPDATED: Key Design Decisions table (lines 77-80) - Added rows for fuzzer sandbox backends
- [x] ALREADY UPDATED: Environment Variables table (line 124) - Added `DCS_FUZZ_CONTAINER_IMAGE`
- [x] ALREADY UPDATED: Development Commands section (lines 127-136) - Added `make build-fuzz-sandbox` and Podman note
- [x] ALREADY UPDATED: Known Limitations (line 145) - Removed item 6 about `deep_scan_fuzz` being deferred (SD-01 is now resolved)

### README.md
- [ ] **Update MCP Tools table** (lines 142-151) - Add `deep_scan_fuzz` tool:
  ```markdown
  | `deep_scan_fuzz` | Run AI-powered fuzzing (requires Podman container backend) |
  ```

- [ ] **Remove stale Known Limitations entry** (line 215) - Delete or update item 7:
  ```markdown
  7. **`deep_scan_fuzz` MCP tool is deferred** — blocked on container-based sandbox backend. Only `deep_scan_fuzz_status` is active. CLI fuzzing uses rlimits-only sandboxing.
  ```
  Should be replaced with:
  ```markdown
  7. **`deep_scan_fuzz` MCP tool requires Podman** — The MCP fuzzing tool uses ContainerBackend for full isolation and is only available when Podman is installed and the `dcs-fuzz-python:latest` image is built via `make build-fuzz-sandbox`. CLI fuzzing supports both SubprocessBackend (rlimits-only) and ContainerBackend.
  ```

- [ ] **Update Installation section** (lines 81-82) - Update the build command to reference the fuzzer-specific target:
  ```markdown
  # Build sandbox images (requires Docker or Podman)
  make build-sandboxes        # Build auditor/architect sandbox images
  make build-fuzz-sandbox     # Build fuzzer sandbox image (Podman only)
  ```

- [ ] **Add DCS_FUZZ_CONTAINER_IMAGE to environment variables** (line 140) - Add missing variable to the Dynamic Analysis section:
  ```markdown
  | `DCS_FUZZ_CONTAINER_IMAGE` | `dcs-fuzz-python:latest` | Podman image used by ContainerBackend for MCP fuzz runs |
  ```

- [ ] **Update Security Model section** (lines 217-221) - Clarify Podman vs Docker usage:
  ```markdown
  The MCP server runs as a **native stdio process** on the host. It does NOT run inside a Docker container, avoiding the Docker socket mount attack vector. Docker or Podman is used for sandbox containers that execute exploit PoCs (auditor/architect phases). The fuzzer's ContainerBackend uses Podman exclusively for rootless container execution.
  ```

## Suggested Updates

### README.md
- [ ] **Add quick reference for fuzzer backend selection** - Add a note in the Quick Start section explaining when each backend is used:
  ```markdown
  # Dynamic analysis backend selection (automatic):
  # - CLI: SubprocessBackend (rlimits) or ContainerBackend if --container flag is used
  # - MCP: ContainerBackend (Podman) only - deep_scan_fuzz tool disabled if Podman unavailable
  ```

- [ ] **Clarify MCP Configuration prerequisites** (lines 86-104) - Add a note about Podman requirement for fuzzing:
  ```markdown
  # MCP Configuration

  **Prerequisites for fuzzing via MCP:**
  - Podman installed (not Docker - rootless execution required)
  - Run `make build-fuzz-sandbox` to build `dcs-fuzz-python:latest` image
  - Set `ANTHROPIC_API_KEY` in environment

  Add to `~/.claude/settings.json`:
  ```

### CLAUDE.md
- [ ] **Add troubleshooting note** - Add to Development Commands section:
  ```markdown
  **Troubleshooting:**
  - If `deep_scan_fuzz` tool is not available in MCP, ensure Podman is installed and `make build-fuzz-sandbox` has been run
  - Check `dcs status` CLI command or `deep_scan_fuzz_status` MCP tool for backend availability
  ```

## Rationale

### Required Updates - README.md

**Commit b3355e1** resolved Security Deviation SD-01 by implementing the ContainerBackend for the fuzzer. This was a major architectural change with the following impacts:

1. **`deep_scan_fuzz` MCP tool is now active** (not deferred) - The tool is conditionally registered when `ContainerBackend.is_available()` returns true (lines 311-358 in `src/deep_code_security/mcp/server.py`). README.md line 215 still states it is "deferred" and "blocked on container-based sandbox backend", which is now incorrect.

2. **New environment variable** - `DCS_FUZZ_CONTAINER_IMAGE` was added to `src/deep_code_security/shared/config.py` (line 92) but is not documented in README.md's environment variables table.

3. **New build target** - `make build-fuzz-sandbox` was added to the Makefile (line 81-84) to build the Podman-specific fuzzer image (`sandbox/Containerfile.fuzz-python`), distinct from the auditor/architect Docker images. README.md line 82 still references generic `make build-sandboxes` without mentioning the fuzzer-specific target.

4. **MCP tools table incomplete** - The `deep_scan_fuzz` tool is now conditionally available but not listed in the MCP Tools table (lines 142-151), creating a gap in the tool inventory.

5. **Podman vs Docker distinction** - The Security Model section (lines 217-221) mentions "Docker/Podman" generically for sandboxes, but per commit b3355e1, the fuzzer specifically uses Podman (not Docker) for rootless execution. This architectural constraint should be explicit.

### Already Updated - CLAUDE.md

CLAUDE.md was already updated in commit b3355e1 and accurately reflects the new architecture:
- Line 33: MCP tool registration logic documented
- Lines 77-80: Design decisions table includes both SubprocessBackend and ContainerBackend
- Line 124: `DCS_FUZZ_CONTAINER_IMAGE` documented
- Lines 127-136: Development Commands section includes `make build-fuzz-sandbox` with Podman note
- Line 145: Known Limitations item 6 removed (SD-01 resolved)

The CLAUDE.md updates demonstrate the correct documentation standard; README.md needs to be brought into alignment.

### Suggested Updates

These are user-facing quality-of-life improvements:
- **Backend selection note**: Clarifies when ContainerBackend vs SubprocessBackend is used (CLI has flexibility, MCP is Podman-only)
- **MCP prerequisites**: Front-loads the Podman setup requirement to avoid user confusion when the `deep_scan_fuzz` tool is missing
- **Troubleshooting**: Helps users diagnose why the MCP tool might not be available

These are not strictly required for accuracy but improve the developer experience.
