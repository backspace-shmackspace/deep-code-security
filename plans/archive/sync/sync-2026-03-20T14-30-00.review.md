# Documentation Sync Review - 2026-03-20T14:30:00

## Verdict
**CURRENT**

## Required Updates
None. Both CLAUDE.md and README.md are accurate and current as of commit c96d028.

## Suggested Updates
None. Documentation accurately reflects the current codebase state.

## Rationale

### Changes Reviewed
The major change since the last sync was commit 992e1e5 ("feat(hunter): add Semgrep scanner backend with tree-sitter fallback"), which introduced a significant architectural change to the Hunter phase. This was documented in commit c96d028 ("docs: sync with codebase").

### Documentation Coverage Analysis

**CLAUDE.md verification:**
1. ✅ **Architecture diagram** (line 22) correctly shows `hunter/` as "scanner backend (Semgrep/tree-sitter) -> taint track -> RawFinding[]"
2. ✅ **Environment variables table** (lines 107-109) includes all three Semgrep-related variables:
   - `DCS_SCANNER_BACKEND` (default: `auto`)
   - `DCS_SEMGREP_TIMEOUT` (default: `120`)
   - `DCS_SEMGREP_RULES_PATH` (default: `<registry>/semgrep`)
3. ✅ **Key Design Decisions table** (line 83) includes the scanner backend decision with correct rationale
4. ✅ **Known Limitations #2** (line 152) correctly notes that query brittleness is "Resolved when using the Semgrep backend"
5. ✅ **Known Limitations #11** (line 161) correctly documents the Semgrep OSS taint path limitation (synthetic two-step, capped at 50 taint completeness score)
6. ✅ All security constraints, code quality patterns, and testing requirements remain accurate
7. ✅ CLI commands unchanged (as intended by the plan)
8. ✅ MCP tools count correct: "6 tools always + deep_scan_fuzz and deep_scan_hunt_fuzz when Podman available" (line 35) — this is still 7 total tools when Podman is available

**README.md verification:**
1. ✅ **Quick Start** (line 18) correctly shows `dcs hunt` with no backend configuration required (auto-selection works)
2. ✅ **Architecture overview** (line 45) correctly updated to "scanner backend (Semgrep/tree-sitter) → source/sink match → taint track"
3. ✅ **Optional Environment Variables** section (lines 152-166) includes all Semgrep variables with correct defaults
4. ✅ **Known Limitations #2** (lines 249-254) correctly documents the tree-sitter-specific brittleness and notes "When using Semgrep backend, aliased imports, fully-qualified names, and class attributes are handled correctly"
5. ✅ Installation instructions (lines 110-124) do NOT require Semgrep as a core dependency (tree-sitter fallback pattern preserved)
6. ✅ MCP tools table (lines 190-200) shows 8 tools total, with `deep_scan_fuzz` and `deep_scan_hunt_fuzz` annotated as requiring Podman

**pyproject.toml verification:**
- ✅ Semgrep added as optional dependency `[project.optional-dependencies.semgrep]` with correct version constraint `>=1.50.0,<2.0.0`
- ✅ NOT added to core dependencies (preserves fallback pattern)

**Registry structure verification:**
- ✅ `registries/semgrep/` directory exists with Python, Go, and C subdirectories
- ✅ Original `registries/*.yaml` tree-sitter registries retained (fallback backend)

**Test coverage verification:**
- ✅ Four new test files created per plan:
  - `test_scanner_backend.py` (backend selection)
  - `test_semgrep_backend.py` (Semgrep-specific tests)
  - `test_treesitter_backend.py` (adapter tests)
  - `test_cross_backend_compat.py` (compatibility tests)

### Commit c96d028 Assessment
The documentation sync commit properly updated:
1. CLAUDE.md Architecture section to reflect scanner backend abstraction
2. CLAUDE.md Environment Variables table with new Semgrep variables
3. CLAUDE.md Key Design Decisions with scanner backend choice
4. CLAUDE.md Known Limitations to note Semgrep resolution of brittleness AND Semgrep OSS taint path limitation
5. README.md architecture diagram
6. README.md environment variables section
7. README.md known limitations section

### Undocumented Features
None found. All implemented features from the semgrep-scanner-backend plan are documented.

### Stale Documentation
None found. The documentation accurately reflects that:
- Tree-sitter is still supported (fallback)
- Semgrep is optional (not required)
- Backend selection is automatic with manual override available
- C conditional-assignment-sanitizer work is preserved in tree-sitter backend (Known Limitation #10 notes partial coverage, which is accurate)

### Accuracy Checks
- ✅ Default values in docs match `config.py` implementation
- ✅ Architecture diagrams match actual module structure
- ✅ CLI commands match `cli.py` implementation
- ✅ MCP tool counts match `mcp/server.py` tool registration
- ✅ Known limitations accurately reflect intraprocedural-only scope
- ✅ Security model accurately describes native stdio MCP deployment

### Conclusion
Both documentation files are accurate and complete. The 2026-03-19 sync (commit c96d028) successfully incorporated all changes from the Semgrep scanner backend implementation (commit 992e1e5). No updates are required at this time.
