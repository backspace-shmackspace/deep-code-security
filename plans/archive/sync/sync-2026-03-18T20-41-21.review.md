# Documentation Sync Review: 2026-03-18T20-41-21

## Verdict
**UPDATES_NEEDED**

## Required Updates

### README.md
- [ ] **Add suppression file feature documentation**. The `.dcs-suppress.yaml` feature was added in commit db210da but is not mentioned anywhere in README.md. This is a significant user-facing feature that should be documented.
  - Add a "Suppression Files" section under "Architecture" or create a new "Features" section
  - Explain basic usage: create `.dcs-suppress.yaml` in project root to suppress findings
  - Mention the `--ignore-suppressions` flag for hunt/full-scan/hunt-fuzz commands
  - Link to example YAML format or reference the plans/ documentation

- [ ] **Add CLI command table entry for --ignore-suppressions**. The Quick Start section shows basic `dcs hunt` and `dcs fuzz` commands, but does not show the new `--ignore-suppressions` flag.
  - Option 1: Add to existing Quick Start examples: `dcs hunt /path/to/project --ignore-suppressions`
  - Option 2: Add a note under the CLI commands that certain commands accept `--ignore-suppressions`

### CLAUDE.md
- [ ] **Update Architecture section to mention suppressions.py**. The Architecture tree-view (lines 17-39) lists `shared/formatters/` but does not mention `shared/suppressions.py` which was added in commit db210da. Add:
  ```
  shared/          # File discovery, language detection, config, JSON output
      formatters/  # Unified formatter registry (text, json, sarif, html)
      suppressions.py  # .dcs-suppress.yaml loading and matching
  ```

- [ ] **Add suppression file feature to File Conventions section** (lines 152-157). Add a bullet:
  - Suppression file always named `.dcs-suppress.yaml` in project root, loaded via `shared/suppressions.py`

## Suggested Updates

### README.md
- [ ] **Add suppression file to Known Limitations section**. The Known Limitations section mentions intraprocedural taint, query brittleness, etc. Consider adding a note that suppressions only apply to SAST findings (not fuzzer crashes), since the plan explicitly lists this as a non-goal.

- [ ] **Add example .dcs-suppress.yaml snippet**. Since this is a new user-facing file format, a minimal example in README.md would improve discoverability. Example:
  ```yaml
  # .dcs-suppress.yaml
  version: 1
  suppressions:
    - rule: CWE-22
      file: "src/config/*.py"
      reason: "Config paths are admin-controlled"
  ```

### CLAUDE.md
- [ ] **Add suppression file to Key Design Decisions table**. The table includes decisions about taint scope, exploit weight, etc. Consider adding:
  | Decision | Choice | Rationale |
  |----------|--------|-----------|
  | Suppression scope | SAST findings only | Fuzzer operates on runtime crashes; separate triage workflow |
  | Suppression inheritance | Project root only | No parent directory search; explicit over implicit |

- [ ] **Add DCS_SUPPRESS_FILENAME environment variable** (if configurable). If the suppression filename is hardcoded to `.dcs-suppress.yaml` and NOT configurable via environment variable, this is not needed. (Based on the code review, `_SUPPRESS_FILENAME` is a module constant, not an env var, so this is NOT needed.)

## Rationale

### Why README.md needs updates
1. **Commit db210da added a significant user-facing feature** (suppression file support) with:
   - New file format (`.dcs-suppress.yaml`)
   - New CLI flags (`--ignore-suppressions` on hunt/full-scan/hunt-fuzz)
   - New MCP parameter (`ignore_suppressions` on deep_scan_hunt/deep_scan_full/deep_scan_hunt_fuzz)
   - Modified output format (all formatters now show suppression stats)

2. **README.md has zero mentions of "suppression"**. Grep search returned no matches for "suppres" (case-insensitive) in README.md.

3. **README.md is the primary user documentation**. The Quick Start, Installation, and MCP Configuration sections are the first-stop documentation for new users. A feature this significant should be discoverable without reading CLAUDE.md or the plans/ directory.

### Why CLAUDE.md needs updates
1. **Architecture section is stale**. The tree-view (lines 17-39) does not list `shared/suppressions.py`, which is a new top-level module in the `shared/` package. The Architecture section should reflect the current codebase structure.

2. **CLI Commands table was updated** (lines 83-99). The `--ignore-suppressions` flag documentation was added for `hunt`, `full-scan`, and `hunt-fuzz` commands in commit db210da, so CLAUDE.md is CURRENT for CLI commands.

3. **File Conventions section does not mention suppression file naming**. The File Conventions section (lines 152-157) documents naming conventions for models.py, orchestrator.py, registries/, and test fixtures. The suppression file has a fixed naming convention (`.dcs-suppress.yaml` at project root) and should be documented here.

### What is already correct
- **CLAUDE.md CLI Commands table** (lines 83-99) was updated in commit db210da and correctly shows `--ignore-suppressions` flags for hunt/full-scan/hunt-fuzz.
- **CLAUDE.md Environment Variables section** (lines 101-130) does not need updates. The suppression filename is not configurable via environment variable (it's a hardcoded constant `_SUPPRESS_FILENAME = ".dcs-suppress.yaml"`).
- **CLAUDE.md Security section** (lines 41-53) does not need updates. The suppressions.py module adheres to all security rules:
  - Uses `yaml.safe_load()` exclusively (line 346 in suppressions.py)
  - No eval/exec/shell usage (documented in suppressions.py docstring, lines 7-10)
  - File size and rule count limits prevent DoS (lines 42-43 in suppressions.py)
  - Path is derived from validated target path with hardcoded filename (lines 11-13 in suppressions.py)

### Reference commits
- **db210da** "feat(shared): add suppression file support (.dcs-suppress.yaml)" -- Added suppressions.py, updated CLI, updated formatters, updated MCP server
- **206c47a** "feat(plans): approve suppressions-file blueprint" -- Approved the plan and feasibility/redteam reviews
- **77404ca** "chore: archive suppressions-file ship artifacts" -- Archived QA report and code review

### Documentation consistency check
The suppression file feature is fully documented in:
- `plans/suppressions-file.md` (design spec)
- `plans/suppressions-file.feasibility.md` (feasibility review)
- `plans/suppressions-file.redteam.md` (security review)
- `plans/archive/suppressions-file/suppressions-file.qa-report.md` (QA report)
- `plans/archive/suppressions-file/suppressions-file.code-review.md` (code review)

However, it is NOT documented in the primary user-facing documentation (README.md), and the CLAUDE.md Architecture and File Conventions sections are incomplete.
