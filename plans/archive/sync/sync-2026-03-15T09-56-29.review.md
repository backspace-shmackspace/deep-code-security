# Documentation Sync Review — 2026-03-15T09:56:29

## Verdict
UPDATES_NEEDED

## Required Updates

### CLAUDE.md
- [x] Already current - accurately reflects fuzzer merge
- [ ] Add missing environment variable: `ANTHROPIC_API_KEY`
- [ ] Add missing environment variable: `GOOGLE_CLOUD_PROJECT`
- [ ] Add missing environment variable: `CLOUD_ML_PROJECT_NUMBER`
- [ ] Add missing environment variable: `ANTHROPIC_VERTEX_PROJECT_ID`
- [ ] Update MCP tools count: "6 tools" → "6 active tools (7th deferred)"
- [ ] Clarify MCP tools list to include `deep_scan_fuzz_status`

### README.md
- [ ] Add Dynamic Analysis (Fuzzing) mode to overview/architecture
- [ ] Add fuzzer CLI commands: `dcs fuzz`, `dcs replay`, `dcs corpus`, `dcs fuzz-plugins`, `dcs report`
- [ ] Remove non-existent CLI command: `dcs remediate` (not implemented)
- [ ] Add MCP tool: `deep_scan_fuzz_status`
- [ ] Add fuzzer environment variables to "Optional Environment Variables" section
- [ ] Add fuzzer-specific environment variables: `ANTHROPIC_API_KEY`, `GOOGLE_CLOUD_PROJECT`, `CLOUD_ML_PROJECT_NUMBER`, `ANTHROPIC_VERTEX_PROJECT_ID`
- [ ] Add optional dependency installation: `pip install -e ".[dev,fuzz]"` for fuzzing support
- [ ] Update development commands to include: `make test-fuzzer`
- [ ] Add note about fuzzer requiring `anthropic` SDK (optional dependency)

## Suggested Updates

### CLAUDE.md
- [ ] Add cross-reference to fuzzer consent model (`~/.config/deep-code-security/consent.json`)
- [ ] Document that fuzzer requires optional `[fuzz]` dependency group
- [ ] Add example of fuzzer-specific config file location (`~/.config/deep-code-security/config.toml`)

### README.md
- [ ] Add "Two Analysis Modes" section explaining Static (SAST) vs Dynamic (Fuzzing)
- [ ] Add fuzzer quick start example
- [ ] Document Vertex AI integration option for fuzzing
- [ ] Add section explaining consent model for API key transmission
- [ ] Add troubleshooting note for fuzzer availability (requires `anthropic` SDK)
- [ ] Consider adding fuzzer output format examples
- [ ] Update "v1.1 Roadmap" to mention fuzzer language expansion (Go, Rust, Java)

## Rationale

### Recent Merge: fuzzy-wuzzy → deep-code-security
**Commit**: `08a9a4c feat: merge fuzzy-wuzzy AI fuzzer as dynamic analysis backend`

This major merge added an entire dynamic analysis backend to the codebase, but the README.md has not been updated to reflect this fundamental architectural change.

#### Evidence from Code:

1. **New CLI commands exist** (verified in `/src/deep_code_security/cli.py`):
   - `dcs fuzz` (line 401)
   - `dcs replay` (line 502)
   - `dcs corpus` (line 585)
   - `dcs fuzz-plugins` (line 616)
   - `dcs report` (line 637)

   **But README.md only shows**: `hunt`, `verify`, `remediate` (line 12-14)
   - Note: `dcs remediate` command does NOT exist in cli.py - only the MCP tool `deep_scan_remediate` exists

2. **New MCP tool exists** (verified in `/src/deep_code_security/mcp/server.py`):
   - `deep_scan_fuzz_status` (line 234-250)

   **But README.md MCP tools section** (line 93-99) lists only 5 tools and doesn't include `deep_scan_fuzz_status`

3. **New environment variables in use** (verified in `/src/deep_code_security/fuzzer/config.py`):
   - `ANTHROPIC_API_KEY` (line 91) - API key for Claude
   - `GOOGLE_CLOUD_PROJECT` (line 132, 141) - GCP project for Vertex AI
   - `CLOUD_ML_PROJECT_NUMBER` (line 133, 142) - Alternative GCP project identifier
   - `ANTHROPIC_VERTEX_PROJECT_ID` (line 134, 140) - Vertex AI project override

   **But neither CLAUDE.md nor README.md document these**

4. **CLAUDE.md is mostly current** - already includes:
   - Fuzzer architecture documentation (line 24-32)
   - Fuzzer CLI commands (line 88-92)
   - Fuzzer environment variables (line 108-117)
   - Security deviations for fuzzer (line 45-46, 77-78, 125-126)
   - Known limitation about `deep_scan_fuzz` MCP tool being deferred (line 126)

5. **Architecture mismatch**:
   - README.md describes a three-phase SAST pipeline only (line 20-34)
   - Actual product now has TWO analysis modes: Static (SAST) + Dynamic (Fuzzing) per CLAUDE.md (line 5-11)

6. **Installation instructions incomplete**:
   - README.md shows `pip install -e ".[dev]"` (line 9, 50)
   - Fuzzing requires optional dependency: `pip install -e ".[dev,fuzz]"` to get `anthropic`, `rich`, `coverage` packages
   - This is documented in the merge plan but not in user-facing docs

### Recent Output Formatters Merge
**Commit**: `7ad6d7f feat: add SARIF 2.1.0 and HTML output formats`

This merge added extensible formatters but documentation was already updated (both docs show `--format` with text/json/sarif/html options).

### Specific Documentation Gaps

**README.md Critical Gaps**:
1. No mention of dynamic analysis/fuzzing mode
2. CLI command reference is stale (shows `remediate` which doesn't exist as CLI command, missing 5 fuzzer commands)
3. MCP tools list incomplete (missing `deep_scan_fuzz_status`)
4. Environment variables section doesn't include fuzzer or API authentication variables
5. Installation doesn't mention `[fuzz]` optional dependency group

**CLAUDE.md Minor Gaps**:
1. Four fuzzer-related environment variables undocumented (API authentication and Vertex AI)
2. MCP tools description says "6 tools" but should clarify "6 active tools (deep_scan_fuzz deferred per SD-01)"

### Priority Assessment

**Critical (blocks user success)**:
- Missing fuzzer CLI commands in README (users won't know they exist)
- Missing `ANTHROPIC_API_KEY` documentation (fuzzing won't work without it)
- Missing `[fuzz]` installation instructions (users will get import errors)

**Important (causes confusion)**:
- README architecture section doesn't mention fuzzing
- Non-existent `dcs remediate` command in README quick start

**Nice-to-have (improves clarity)**:
- Vertex AI environment variables documentation
- Fuzzer consent model cross-reference
- Test command updates
