# Documentation Sync Review — 2026-03-13T09-45-00

## Recent Changes (Last 5 Commits)

```
07efac9 chore: update project learnings from /ship run
3a34a75 chore: archive /ship review artifacts for deep-code-security
f0fbc4e feat: implement deep-code-security SAST tool with MCP server
dbcfb67 feat: initialize deep-code-security project
```

### Key Files Added/Modified:
- Core implementation: All `src/deep_code_security/` files
- Configuration: `pyproject.toml`, `Makefile`, `CLAUDE.md`, `README.md`
- Registries: `registries/*.yaml`
- Sandboxes: `sandbox/Dockerfile.*`
- Tests: Complete test suite in `tests/`
- Learnings: `.claude/learnings.md`

## Environment Variables Detected

Found in `src/deep_code_security/shared/config.py`:

1. `DCS_REGISTRY_PATH` (default: `./registries`) — DOCUMENTED ✓
2. `DCS_ALLOWED_PATHS` (default: cwd) — DOCUMENTED ✓
3. `DCS_SANDBOX_TIMEOUT` (default: `30`) — DOCUMENTED ✓
4. `DCS_CONTAINER_RUNTIME` (default: `auto`) — DOCUMENTED ✓
5. `DCS_MAX_CONCURRENT_SANDBOXES` (default: `2`) — DOCUMENTED ✓
6. `DCS_MAX_FILES` (default: `10000`) — DOCUMENTED ✓
7. **`DCS_MAX_RESULTS` (default: `100`) — UNDOCUMENTED ✗**
8. **`DCS_MAX_VERIFICATIONS` (default: `50`) — UNDOCUMENTED ✗**
9. **`DCS_QUERY_TIMEOUT` (default: `5.0`) — UNDOCUMENTED ✗**
10. **`DCS_QUERY_MAX_RESULTS` (default: `1000`) — UNDOCUMENTED ✗**

## Verdict

**UPDATES_NEEDED**

## Required Updates

### CLAUDE.md

- [x] Add missing environment variables to the Environment Variables table:
  - `DCS_MAX_RESULTS` — Max findings returned per hunt operation
  - `DCS_MAX_VERIFICATIONS` — Max findings to verify in auditor phase
  - `DCS_QUERY_TIMEOUT` — Tree-sitter query timeout in seconds
  - `DCS_QUERY_MAX_RESULTS` — Max results per tree-sitter query

### README.md

- [x] Add missing environment variables to MCP Configuration example:
  - Document the additional configuration options available
  - Add a note about optional environment variables for advanced tuning

## Suggested Updates

### CLAUDE.md
- [ ] Consider adding a section on query performance tuning using `DCS_QUERY_TIMEOUT` and `DCS_QUERY_MAX_RESULTS`

### README.md
- [ ] Consider adding an "Environment Variables" section with all available variables (not just the MCP example)
- [ ] Consider adding troubleshooting section for common sandbox/container issues

## Rationale

### Missing Environment Variables
The implementation in `src/deep_code_security/shared/config.py` (lines 43-51) defines four additional environment variables that control operational limits:

1. **`DCS_MAX_RESULTS`** (line 44): Limits the number of findings returned by the hunter phase to prevent overwhelming output
2. **`DCS_MAX_VERIFICATIONS`** (line 45): Limits the number of findings that will be verified in the auditor phase (relevant since sandbox execution is expensive)
3. **`DCS_QUERY_TIMEOUT`** (line 48-50): Sets timeout for individual tree-sitter queries to prevent DoS from pathological inputs
4. **`DCS_QUERY_MAX_RESULTS`** (line 51): Caps results per query to prevent memory exhaustion

These variables are functional and affect runtime behavior, but are not documented in either CLAUDE.md or README.md. Users may need to tune these values for large codebases or performance-constrained environments.

### Documentation Currency
Both CLAUDE.md and README.md were created in commit `f0fbc4e` and accurately reflect the architecture, security model, and core functionality. The only gap is the four environment variables listed above.

The learnings file (`.claude/learnings.md`) from commit `07efac9` does not require CLAUDE.md/README.md updates — it's internal project knowledge.
