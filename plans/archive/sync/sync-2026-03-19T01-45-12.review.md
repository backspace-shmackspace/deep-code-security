# Documentation Sync Review - 2026-03-19T01-45-12

## Verdict
**UPDATES_NEEDED**

## Required Updates

### README.md

- [ ] **Line 70: Update language support statement** - Currently lists C as "stretch goal — basic argv/stdin patterns". Per commit 0648653, C language support is now fully implemented with 7 CWE sink categories (command_injection, buffer_overflow, format_string, path_traversal, memory_corruption, integer_overflow, dangerous_function) plus 4 source categories (argv, gets, fgets, getenv). Change to: "C (command injection, buffer overflow, format string, memory corruption patterns)"

- [ ] **Line 66-71: Remove "Supported Languages (v1)" header nesting** - The "Supported Languages" section appears within the "Dynamic Analysis (Fuzzing)" section but covers SAST. Consider moving it to its own top-level section or placing it after the architecture overview.

- [ ] **Add C-specific Known Limitations section** - README.md Known Limitations (lines 241-263) does not mention any C-specific constraints. CLAUDE.md lines 152-156 document critical C limitations that should be reflected in README.md:
  - No preprocessor resolution (`#ifdef` guards invisible)
  - No struct member taint tracking
  - Pointer aliasing tracked within same function only
  - Output-parameter sources (recv, fread, read, scanf) deferred (limitation #7)
  - CWE-416 (use-after-free) detection deferred (limitation #8)
  - mktemp()/tmpnam() detection gap (limitation #9)

### CLAUDE.md

- [ ] **No changes required** - CLAUDE.md was updated in commit 0648653 and already includes comprehensive C language documentation in Known Limitations section (lines 152-156).

## Suggested Updates

### README.md

- [ ] **Add C language examples to Quick Start** - Consider adding a C-specific example to the Quick Start section (lines 9-35) to demonstrate C scanning capability:
  ```bash
  # Scan C project for vulnerabilities
  dcs hunt /path/to/c/project
  ```

- [ ] **Document C test fixtures** - The project now has 7 C test fixtures in `tests/fixtures/vulnerable_samples/c/` and 2 safe samples. Consider mentioning these in development/testing documentation.

- [ ] **Clarify "stretch goal" terminology** - Line 70 marks C as "stretch goal" which implies aspirational/incomplete status. Given full implementation with comprehensive test coverage (test_c_registry.py, test_taint_c_paths.py), this terminology is misleading.

## Rationale

### Commit 0648653 Analysis

Commit `0648653` ("feat(hunter): add C language SAST support with 7 CWE sink categories") introduces:

1. **Full C language registry** (`registries/c.yaml` v2.0.0):
   - 7 sink categories covering CWE-78, CWE-120, CWE-119, CWE-134, CWE-190, CWE-676, CWE-22
   - 4 source patterns (argv, gets, fgets, getenv)
   - 7 sanitizer patterns (snprintf, strncpy, strlcpy, etc.)
   - Comprehensive Known Limitations documentation in YAML comments

2. **Hunter modifications**:
   - `orchestrator.py`: C language added to supported languages set
   - `taint_tracker.py`: C-specific taint tracking logic
   - `file_discovery.py`: `.c` and `.h` file extension support

3. **Test coverage**:
   - 7 vulnerable C fixtures covering all CWE categories
   - 2 safe C fixtures demonstrating proper sanitization
   - `test_c_registry.py`: 24 test cases for registry loading and query compilation
   - `test_taint_c_paths.py`: taint flow validation

4. **Documentation updates**:
   - `CLAUDE.md` lines 152-156 added (C-specific Known Limitations)
   - Plan documents in `plans/c-language-support.*`

### Gap Analysis

**README.md** has not been updated to reflect this shipped feature:
- Still describes C as "stretch goal" (line 70)
- No mention of C-specific CWE categories supported
- No mention of C-specific limitations (preprocessor, output parameters, use-after-free)

**CLAUDE.md** is current and accurate. No changes needed.

### User Impact

Users reading README.md will underestimate C language support maturity. The "stretch goal" label suggests incomplete or experimental status, when in fact C support has:
- Production-ready registry with comprehensive CWE coverage
- 90%+ test coverage (per project requirements)
- Full integration with hunter/auditor/architect pipeline
- Documented limitations and known gaps

This creates a documentation-code drift that may discourage C codebase scanning or lead to incorrect capability expectations.
